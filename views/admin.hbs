<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Кабинет</title>
    <link rel="stylesheet" href="styles.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" 
    rel="stylesheet" 
    integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" 
    crossorigin="anonymous">
</head>
<body>
    <div class="container">
        <nav class="navbar navbar-expand-lg border-bottom border-5 border-gray  mb-5">
            <div class="container-fluid">
                <a class="navbar-brand" href="/">
                    <img src="/images/Logotype.png" alt="dog" width="80" height="80">
                </a>
                    <a class="navbar-brand d-flex align-items-center rounded-pill" href="/profile">
                        <img src="/images/Logotype.png" alt="dog" width="50" height="50">
                        <p class="mb-0 ms-2">Профиль</p>
                    </a>
            </div>
        </nav>
        <table class="table mt-5">
            <thead>
                <tr>
                    <th scope="col">#</th>
                    <th scope="col">Фото</th>
                    <th scope="col">Имя</th>
                    <th scope="col">Возраст</th>
                    <th scope="col">Пол</th>
                    <th scope="col">Порода</th>
                    <th scope="col">Статус</th>
                </tr>
            </thead>
            <tbody>
                {{#each dogs}}
                <form method="post">
                    <tr>
                        <th scope="row">{{this._id}}</th>
                        <td><img src="{{this.img}}" alt="" width="30px" height="30px"></td>
                        <td>{{this.nick}}</td>
                        <td>{{this.age}}</td>
                        {{#if (EqualTerms this.sex "male")}}
                        <td class="table-primary">{{this.sex}}</td>
                        {{else}}
                        <td class="table-danger">{{this.sex}}</td>
                        {{/if}}
                        <td>{{this.breed}}</td>
                        {{#if (EqualTerms this.isHome this.isAdmin)}}
                        <td class="table-success">Нашла себе дом!</td>
                        {{else}}
                        <td><button class="btn btn-outline-warning" type="submit" value="{{this._id}}" name="sendDog">Выдать</button></td>
                        {{/if}}
                    </tr>
                </form>
                {{/each}}
            </tbody>
        </table>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" 
    integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" 
    crossorigin="anonymous"></script>
</body>
</html>